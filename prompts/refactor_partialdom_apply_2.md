##Goal

 As we speak, we need to modify the 'apply' phase for apply pilot, specifically looking at the @src/applypilot/apply/launcher.py and @src/applypilot/apply/prompt.py file to make use of the new sqlite column 'application_schema' which contains all the questions that each job site will show. We need to also create a new column in the sqlite database called 'application_prepopulated' which will house a JSON map of the application we wish to submit prior to attempting to submit it on the browser.

## Instructions
1.	Study the structure of the sample JSON file @test_scripts/sample_application_details_getresponse.JSON to understand generally how the application schema is structured. Analyze patterns for how the data is stored within the fields 'compliance', 'demographic questions', and 'questions'. We should note all questions that are mandatory since these are the applications we need to populate at a minimum. This structure is generally followed within the sqlitedatbase column 'application_schema', where we are storing a JSON template for each job application that is generated during the 'enrich' phase.

2.	In launcher.py, create a new function called 'prepopulate_app' which compares the data under 'application_schema' in the sqlite database against the personal information data that I have stored in my internal 'profile.JSON' (‪C:\Users\manue\.applypilot\profile.json). The function should parse through both files and create a clean map of the applications required questions, key identifiers ['labels', 'fields',  'required'], and the question ids ('question_11657194007', 'question_11657195007'…etc). These question IDs will be critical to being able to locate the right fields during DOM analysis by the LLM in later workflow stages. The function should return this map, which would include both required questions that need to be populated, along with their respective answers using the 'profile.JSON' file. The answers can be saved for each required questions under an element 'applicant_answer'. We will call this map the 'application_prepopulated', and we will need to add it to a new column called 'application_prepopulated' in the slqite database after an LLM checks it (more on those details below). If an answer to a question could not be parsed, mark the 'applicant_answer' as 'answer_not_found'. 

3.	Lets create a new LLM function 'check_prepopulated_app' which will check how we populated the 'application_propopulated'. The prompt for this function should instruct the gemini LLM to look at the data from the 'application_schema', the data from 'profile.json' and the current map that the prior function 'prepopulate_app' generated. The LLM should verify, correct, and include any details from analyzing these files to ensure the 'prepopulated_app' file is correct, that all required questions have an 'applicant_answer'. The llm should note any questions the prior function answered with 'answer_not_found' and proceed to analyze if the LLM can answer them. Very importantly, we need to make sure we provide a path to upload my resume. Resumes should ALWAYS be uploaded, irrespective of whether the corresponding question is marked  required: True or required: False. Once the LLM has returned a correct and cleaned 'propulated_app', the 'check_prepopulated_app' function should write this data into a column 'application_prepopulated' within the sqlitedatabase. The function 'check_prepopulated_app' should live in a new module @src/applypilot/apply/check_propulatedapp_prompt.py, and should be activated by @src/applypilot/apply/launcher.py.

4.	Modify the existing Javascript injection logic so that we can populate all generic fields and as many custom fields as possible using the 'preopulated_app'. This data would already be stored in the 'application_prepopulated' column of the sqlite database. The logic should mark any fields from application_prepopulated that could not be successfully injected into the browser context. The function should return the 'preopulated_app' with these marks so that the LLM can take over in the next phase.

5.	As part of our 'partial-dom-parsing' logic which involves passing onto the gemini LLM anything that could not be resolved during the Javascript injection, lets also feed the final 'preopulated_app' to the LLM that will ultimately inspect the DOM and do the final application population on the browser. The LLM should be able to assess the difficulty of matching the correct dom elements against the 'preopulated_app' to populate the answers. The LLM should first scan the DOM structure and determine whether it would take longer than 15 seconds to populate the app. If so, the LLM should give up on attempting to populate the form, and the logic should return an error for this application to be registered in the column 'apply_error' of the sqlite database. If the LLM can populate it, then it should proceed to do so. As a final check, the LLM should create one last field within the 'prepopulated_app' map that has a key 'llm_found_submit', and fill this out with any of the following values: 
 - 'submit_not_found': for any instances where the submission button was not identified 
 - 	'submit_found_dry_run': for any instances where the submission button was identified but not pressed because applypilot was being run as a '—dry-run'
 - 	'submit_pressed'.  For any instances where the submission button was identified and pressed so that the app could submit.

6.	Once an application has been populated, lets write the final 'populated_app'  (which is the 'prepopulated_app, but upon confirmation of the LLM that we have successfully applied) onto the column 'application_schema'. This final schema should be readable, including both the question id ('question_11657195007') and the label ('Will you now or in the future require visa sponsorship from Esri for employment (e.g., H-1B visa)?') so that I can analyze the accuracy of what was submitted later.

## Final points.
1.	Lets make sure that all sql statements manipulating the SQLITEdatabse are parameterized. Currently this is not on all files reading and writing from the database.
2.	When navigating to each application while using playwright, incorporate use a wait condition like networkidle or look for a universal form element (like the submit button or file upload input) to ensure the heavy, dynamic tracking scripts are fully loaded
3.	Do not run any applypilot jobs since it is likely these commands will hang due to limitations that the gemini cli has in manipulating the browser.
4.	Lets ensure all applypilot run apply command can work, for the standard 'apply' command, as well as for '—dry-run', '-c', '—min-score' combinations.
